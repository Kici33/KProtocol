{
  "name": "kprotocol",
  "private": true,
  "description": "Dev dependency for regenerating the packet catalog from minecraft-data",
  "scripts": {
    "generate": "node tools/generate_packets.mjs --out generated --versions all-known",
    "embed:translation": "node tools/embed_translation_mappings.mjs"
  },
  "devDependencies": {
    "minecraft-data": "^3.84.0"
  }
}
